# Rainwoo Design LAB Website

Static portfolio website for Rainwoo Design LAB.

## Deploy

This project can be deployed directly with GitHub Pages. The GitHub Actions workflow in `.github/workflows/pages.yml` publishes the repository root as a static site.

For a custom domain, add the domain to GitHub Pages settings and create a matching Cloudflare DNS record.
